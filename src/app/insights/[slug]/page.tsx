import {notFound} from 'next/navigation';import {prisma} from '@/lib/prisma';import {RenderBlocks} from '@/lib/blocks'
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await prisma.article.findUnique({where:{slug},select:{title:true,seoTitle:true,seoDescription:true,coverImage:true,noIndex:true}});if(!a)return{};return{title:a.seoTitle||a.title,description:a.seoDescription||undefined,robots:a.noIndex?{index:false,follow:false}:undefined,openGraph:{title:a.seoTitle||a.title,description:a.seoDescription||undefined,images:a.coverImage?[a.coverImage]:undefined}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await prisma.article.findUnique({where:{slug}});if(!a||a.status!=='PUBLISHED')notFound();return <main className="px-6 pb-32 pt-40"><article className="mx-auto max-w-4xl"><div className="pill inline-flex">{a.category||'INSIGHTS'}</div><h1 className="mt-7 text-5xl font-semibold tracking-[-.05em] md:text-7xl">{a.title}</h1>{a.excerpt&&<p className="mt-7 max-w-3xl text-xl text-white/55">{a.excerpt}</p>}{a.coverImage&&<img src={a.coverImage} alt="" className="mt-12 w-full rounded-[2rem] border border-white/10"/>}<div className="mt-16"><RenderBlocks blocks={a.content}/></div></article></main>}

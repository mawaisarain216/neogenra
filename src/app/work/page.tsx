import { getCmsPage } from '@/lib/cms-page';import { RenderBlocks } from '@/lib/blocks';
import type {Metadata} from 'next';import {projects} from '@/lib/content';import ProjectCard from '@/components/ProjectCard';export const metadata:Metadata={title:'Work',description:'Selected Neogenra projects and case studies.'};export default async function Work(){const cms=await getCmsPage("work");if(cms)return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><RenderBlocks blocks={cms.content}/></div></main>;return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><div className="pill mb-7 inline-flex text-xs uppercase tracking-[.2em]">Selected work</div><h1 className="section-title max-w-4xl">Work that makes<br/><span className="text-white/35">businesses move.</span></h1><p className="mt-8 max-w-2xl text-lg leading-8 text-white/50">Brand systems, campaigns, digital experiences and growth work built around real business problems.</p><div className="mt-16 grid gap-5 md:grid-cols-2">{projects.map((p,i)=><ProjectCard key={p.title} p={p} i={i}/>)}</div></div></main>}

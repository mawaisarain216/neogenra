import {redirect} from 'next/navigation';import {getCurrentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {RenderBlocks} from '@/lib/blocks'
export default async function Preview({params}:{params:Promise<{slug:string}>}){const user=await getCurrentUser();if(!user)redirect('/admin/login');const {slug}=await params;const page=await prisma.page.findUnique({where:{slug}});if(!page)return <main className="px-6 pt-40">Preview not found.</main>;return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><div className="pill mb-7 inline-flex">DRAFT PREVIEW · {page.status}</div><h1 className="section-title">{page.title}</h1><div className="mt-12"><RenderBlocks blocks={page.content}/></div></div></main>}

import type {MetadataRoute} from 'next';import {prisma} from '@/lib/prisma'
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://neogenra.com';const [pages,articles,projects]=await Promise.all([prisma.page.findMany({where:{status:'PUBLISHED',noIndex:false},select:{slug:true,updatedAt:true}}).catch(()=>[]),prisma.article.findMany({where:{status:'PUBLISHED',noIndex:false},select:{slug:true,updatedAt:true}}).catch(()=>[]),prisma.project.findMany({where:{status:'PUBLISHED'},select:{slug:true,updatedAt:true}}).catch(()=>[])]);return [{url:base,lastModified:new Date()},...pages.filter(p=>p.slug!=='home').map(p=>({url:`${base}/${p.slug}`,lastModified:p.updatedAt})),...articles.map(a=>({url:`${base}/insights/${a.slug}`,lastModified:a.updatedAt})),...projects.map(p=>({url:`${base}/work/${p.slug}`,lastModified:p.updatedAt}))]}

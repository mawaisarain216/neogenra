'use client'
import {FormEvent,useState} from 'react'
export default function Contact(){const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);const payload={name:String(form.get('name')||''),email:String(form.get('email')||''),company:String(form.get('company')||''),service:String(form.get('service')||''),message:String(form.get('message')||''),website:String(form.get('website')||'')};try{const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error();setSent(true)}catch{setError('We could not send your brief. Please try again.')}finally{setBusy(false)}}return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><div className="pill mb-7 inline-flex text-xs uppercase tracking-[.2em]">Start a project</div><h1 className="section-title max-w-5xl">Tell us what<br/><span className="text-white/35">you&apos;re building.</span></h1>{sent?<div className="glass mt-16 rounded-[2rem] p-10"><h2 className="text-3xl font-semibold">Brief received.</h2><p className="mt-3 text-white/50">Thanks — your project brief is now securely in the Neogenra workspace.</p></div>:<form onSubmit={submit} className="mt-16 grid gap-5 md:grid-cols-2"><input name="name" required placeholder="Your name" className="glass rounded-2xl px-5 py-4 outline-none"/><input name="email" required type="email" placeholder="Email" className="glass rounded-2xl px-5 py-4 outline-none"/><input name="company" placeholder="Company" className="glass rounded-2xl px-5 py-4 outline-none"/><select name="service" className="glass rounded-2xl px-5 py-4 outline-none bg-[#0b0b0c]"><option value="">What do you need?</option><option>Brand</option><option>Website</option><option>Growth</option><option>AI / Tech</option></select><textarea name="message" required placeholder="Tell us about the problem, goal and timeline" className="glass min-h-48 rounded-2xl px-5 py-4 outline-none md:col-span-2"/><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="hidden"/><button disabled={busy} className="w-fit rounded-full bg-white px-7 py-4 font-semibold text-black disabled:opacity-50">{busy?'Sending…':'Send project brief →'}</button>{error&&<p role="alert" className="text-sm text-red-300 md:col-span-2">{error}</p>}</form>}</div></main>}

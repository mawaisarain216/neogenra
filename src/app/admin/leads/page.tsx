import { prisma } from '@/lib/prisma'
export default async function Leads(){const items=await prisma.lead.findMany({orderBy:{createdAt:'desc'},take:100});return <main className="admin-shell"><p className="eyebrow">CRM</p><h1>Leads</h1><div className="mt-8 grid gap-3">{items.map(x=><div key={x.id} className="glass-panel"><div className="flex justify-between"><strong>{x.name}</strong><span className="text-xs text-white/40">{x.status}</span></div><p className="mt-2 text-white/55">{x.message}</p><small className="mt-3 block text-white/30">{x.email??'No email'} · {x.company??'No company'}</small></div>)}</div></main>}

import {prisma} from '@/lib/prisma'
export default async function Audit(){const items=await prisma.auditLog.findMany({orderBy:{createdAt:'desc'},take:200,include:{user:{select:{email:true,name:true}}}});return <main className="admin-page"><div className="admin-heading"><div><p className="eyebrow">GOVERNANCE</p><h1>Audit log</h1><p>Security and content changes across the CMS.</p></div></div><div className="glass-panel overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="text-white/40"><th className="p-3">Time</th><th className="p-3">Action</th><th className="p-3">Entity</th><th className="p-3">User</th></tr></thead><tbody>{items.map(x=><tr className="border-t border-white/10" key={x.id}><td className="p-3">{x.createdAt.toISOString()}</td><td className="p-3">{x.action}</td><td className="p-3">{x.entity||'—'} {x.entityId||''}</td><td className="p-3">{x.user?.name||x.user?.email||'system'}</td></tr>)}</tbody></table></div></main>}

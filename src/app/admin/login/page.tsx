export default function AdminLogin(){return <main className="admin-login"><div className="glass-panel"><p className="eyebrow">NEOGENRA ADMIN</p><h1>Secure sign in</h1><form method="post" action="/api/auth/login"><label>Email<input name="email" type="email" autoComplete="username" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label><label>Authenticator code <span className="text-xs text-white/40">(required when MFA is enabled)</span><input name="otp" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code" /></label><button type="submit">Sign in</button></form></div></main>}

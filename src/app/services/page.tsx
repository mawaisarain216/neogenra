import { getCmsPage } from '@/lib/cms-page';import { RenderBlocks } from '@/lib/blocks';
import type {Metadata} from 'next';import {services} from '@/lib/content';export const metadata:Metadata={title:'Services',description:'Brand, creative, digital, growth and AI services from Neogenra.'};export default async function Services(){const cms=await getCmsPage("services");if(cms)return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><RenderBlocks blocks={cms.content}/></div></main>;return <main className="px-6 pb-32 pt-40"><div className="mx-auto max-w-6xl"><div className="pill mb-7 inline-flex text-xs uppercase tracking-[.2em]">Capabilities</div><h1 className="section-title max-w-5xl">Everything a modern<br/><span className="text-white/35">brand needs next.</span></h1><div className="mt-20 divide-y divide-white/10">{services.map(([n,d],i)=><section key={n} className="grid gap-6 py-12 md:grid-cols-[100px_1fr_1fr]"><span className="text-white/30">0{i+1}</span><h2 className="text-4xl font-semibold tracking-[-.04em]">{n}</h2><p className="max-w-lg text-lg leading-8 text-white/50">{d}</p></section>)}</div></div></main>}

'use client'
import Link from 'next/link';import {Menu,X} from 'lucide-react';import {useState} from 'react'
export default function HeaderClient({links,logo}:{links:string[][];logo:string}){const [open,setOpen]=useState(false);return <header className="fixed top-4 left-0 right-0 z-50 px-4"><nav className="glass mx-auto flex max-w-6xl items-center justify-between rounded-full px-4 py-3"><Link href="/" className="font-black tracking-[-.06em] text-xl">{logo}</Link><div className="hidden md:flex items-center gap-5 text-sm text-white/75">{links.map(([n,h])=><Link key={h} href={h} className="hover:text-white">{n}</Link>)}</div><div className="flex items-center gap-2"><Link href="/contact" className="hidden sm:block rounded-full bg-white px-4 py-2 text-sm font-semibold text-black">Let&apos;s talk</Link><button aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(!open)} className="glass rounded-full p-2 md:hidden">{open?<X size={18}/>:<Menu size={18}/>}</button></div></nav>{open&&<div className="glass mx-auto mt-2 max-w-6xl rounded-3xl p-5 md:hidden">{links.concat([['Contact','/contact']]).map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="block border-b border-white/10 py-4 text-lg">{n}</Link>)}</div>}</header>}

'use client'
import { FormEvent, useState } from 'react'
export default function CmsContactForm({title='Tell us what you are building.'}:{title?:string}){const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:String(f.get('name')||''),email:String(f.get('email')||''),company:String(f.get('company')||''),service:String(f.get('service')||''),message:String(f.get('message')||''),website:String(f.get('website')||'')})});if(!r.ok)throw new Error();setSent(true)}catch{setError('We could not send your brief. Please try again.')}finally{setBusy(false)}}if(sent)return <div className="cms-cta"><h2>Brief received.</h2><p className="cms-lede">Thanks — your project brief is now securely in the Neogenra workspace.</p></div>;return <section className="cms-contact"><h2>{title}</h2><form onSubmit={submit} className="cms-contact-grid"><input name="name" required placeholder="Your name"/><input name="email" required type="email" placeholder="Email"/><input name="company" placeholder="Company"/><select name="service" defaultValue=""><option value="">What do you need?</option><option>Brand</option><option>Website</option><option>Growth</option><option>AI / Tech</option></select><textarea name="message" required placeholder="Tell us about the problem, goal and timeline"/><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="honeypot"/><button disabled={busy} className="magnetic-cta">{busy?'Sending…':'Send project brief →'}</button>{error&&<p role="alert" className="cms-error">{error}</p>}</form></section>}

import Link from 'next/link';import type {Project} from '@/lib/content';export default function ProjectCard({p,i}:{p:Project;i:number}){return <Link href={`/work/${p.title.toLowerCase().replaceAll(' ','-')}`} className="group block"><div className="relative aspect-[4/3] overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-br from-white/10 via-white/[.02] to-transparent p-6"><div className="absolute inset-0 opacity-50 transition duration-700 group-hover:scale-110" style={{background:`radial-gradient(circle at ${25+i*17}% ${25+i*9}%, rgba(${80+i*25},${140+i*10},255,.4), transparent 35%)`}}/><div className="relative flex h-full flex-col justify-between"><span className="pill w-fit text-xs">{p.tag}</span><div><div className="text-3xl font-bold tracking-[-.04em]">{p.title}</div><div className="mt-2 text-sm text-white/50">{p.category} · {p.year}</div></div></div></div></Link>}
